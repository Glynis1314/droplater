# === common ===
NODE_ENV=development
TZ=UTC

# === API ===
API_PORT=3000
MONGO_URL=mongodb://mongo:27017/droplater
REDIS_URL=redis://redis:6379
ADMIN_TOKEN=changeme-supersecret
RATE_LIMIT_PER_MINUTE=60
CORS_ORIGIN=*

# === WORKER ===
POLL_INTERVAL_MS=5000
BACKOFF_MS=1000,5000,25000
MAX_ATTEMPTS=3

# === SINK ===
SINK_PORT=4000
SINK_FAIL_MODE=false # set true to force 500s for testing
SINK_IDEMP_TTL_SECONDS=86400
